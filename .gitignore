# OS
.DS_Store

# Dependencies and build output. Project-specific ignores live alongside each
# project (see app/.gitignore); these are the belt-and-braces repo-wide rules.
node_modules/
.next/
out/
.env*
!.env.example